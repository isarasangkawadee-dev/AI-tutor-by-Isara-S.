import type { ReactNode } from "react";
export const metadata = { title: "AI Tutor & Exam Platform", description: "Production integration baseline" };
export default function RootLayout({children}:{children:ReactNode}){return <html lang="th"><body style={{fontFamily:"system-ui",margin:0,background:"#f7f9fc",color:"#172033"}}>{children}</body></html>}
